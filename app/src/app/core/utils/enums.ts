export enum ActionDBEnum {
    CREATE= 1,
    UPDATE = 2,
    DELETE = 3
}

export enum PageEnum {
    HOME = 1
}

export enum ToastTypeEnum {
    SUCCESS = 'success',
    WARNING = 'warning',
    DANGER = 'danger',
    INFO = 'primary'
}

export enum ModalOutputEnum {
    SAVE = 1,
    CANCEL = 2
}
